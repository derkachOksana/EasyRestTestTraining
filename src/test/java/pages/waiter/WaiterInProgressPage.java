package pages.waiter;

import org.openqa.selenium.WebDriver;

public class WaiterInProgressPage extends WaiterMainPage {
    public WaiterInProgressPage(WebDriver driver)   {
        super(driver);
    }
}
