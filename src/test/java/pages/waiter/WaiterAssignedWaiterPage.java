package pages.waiter;

import org.openqa.selenium.WebDriver;

public class WaiterAssignedWaiterPage extends WaiterMainPage{
    public WaiterAssignedWaiterPage(WebDriver driver)   {
        super(driver);
    }
}
